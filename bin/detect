#!/bin/sh
# Usage: bin/compile <build-dir>

if [ -f $1/config/public/wp-config.php ]; then
  echo "WordPress - but this buildpack is now disabled. Migrate to the heroku-php buildpack. //Okko"
  exit 1
else
  exit 1
fi
